import PropTypes from 'prop-types'
import React, { Component } from 'react'

export default class AbstractMenuBar extends Component {
  // static propTypes = {second: third}

  render() {
    return (
      <div>AbstractMenuBar</div>
    )
  }
}
